streamlit>=1.33
pandas>=2.0
plotly>=5.20
yfinance>=0.2.40
requests>=2.31
python-dotenv>=1.0
feedparser>=6.0
# Optional: enables the Claude-researched market synopsis (needs ANTHROPIC_API_KEY).
# Without it, the synopsis falls back to a keyless extractive summary.
anthropic>=0.40
